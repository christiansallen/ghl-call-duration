{
  "name": "ghl-call-duration",
  "version": "1.0.0",
  "description": "GHL Marketplace app — Custom workflow trigger that exposes call duration for branching logic",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "crypto-js": "^4.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
